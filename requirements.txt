# CPU-only torch is installed by default. For NVIDIA GPU support see the
# "GPU / CUDA setup" section in README.md before running pip install.
torch>=2.0
transformers>=4.45
accelerate>=1.0
datasets>=2.18
evaluate>=0.4
seqeval>=1.2.2
spacy>=3.7
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13
pyyaml>=6.0
tqdm>=4.66
kaggle>=1.6
